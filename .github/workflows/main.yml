
name: Flutter Build and Send
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.0'
      - name: Clean AI Code
        run: |
          sed -i '/```/d' lib/main.dart
          sed -i '/^dart$/d' lib/main.dart
          sed -i '1{/^$/d}' lib/main.dart
      - name: Build APK
        run: flutter build apk --debug
      - name: Send APK to Telegram
        run: |
          curl -v -F "chat_id=1857967143" -F "document=@build/app/outputs/flutter-apk/app-debug.apk" -F "caption=🚀 تم بناء تطبيقك بنجاح!" [https://api.telegram.org/bot](https://api.telegram.org/bot)8247992344:AAHUK1O7IqQJyeZ44TOh7v93kgWtb6yA1WU/sendDocument
