
import 'package:flutter/material.dart';
// Build ID: mekf
// هذا الكود مختلف تماماً عن العداد
void main() => runApp(MaterialApp(
  home: Scaffold(
    appBar: AppBar(title: Text('FastBridge mekf')),
    body: Center(child: Icon(Icons.send, size: 100, color: Colors.blue)),
  ),
));
